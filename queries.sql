CREATE TABLE Employees (
EmployeeID INTEGER PRIMARY KEY AUTOINCREMENT,
FirstName TEXT, 
LastName TEXT,
JobDescription TEXT,
);
CREATE TABLE Equipment (
EquipmentID INTEGER PRIMARY KEY AUTOINCREMENT,
Description TEXT  
),
INSERT INTO Employees (FirstName, LastName, JobDescription) VALUE("Rico", "Marko" "LinuxEmployee" );
INSERT INTO Equipment (Description) VALUE ("PC")

